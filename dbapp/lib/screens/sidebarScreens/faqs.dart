import 'package:flutter/material.dart';

class FAQS extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(title: new Text("FAQs"), backgroundColor: Colors.redAccent),
      body: new Center(
        child: Text("FAQS GO HERE"),
      ),
    );
  }
}